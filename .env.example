SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
SHOPIFY_APP_URL=https://your-app.example.com
SCOPES=read_orders,read_fulfillments
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/tax_invoice
NODE_ENV=development
RESEND_API_KEY=
EMAIL_FROM=Tax Invoice <invoices@your-domain.example.com>

# Deploy credential, not an app runtime variable. Wrangler reads it from this
# file, which overrides any global `wrangler login` for this project only.
# See docs/deployment.md -> "Authenticating the deploy".
CLOUDFLARE_API_TOKEN=
